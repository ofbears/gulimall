spring:
  application:
    name: gulimall-coupon
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        #配置这个可以启用yaml格式文件作为配置文件,默认为properties
        file-extension: yaml
        namespace: e4d84c4c-5f4a-4c7d-bf09-f1d28e38ab07
  profiles:
    active: dev